import { SubscriptionsView } from "@/components/subscriptions/subscriptions-view";

export const metadata = { title: "Subscriptions — Pkay" };

export default function SubscriptionsPage() {
  return <SubscriptionsView />;
}