import { ApiReferenceView } from "@/components/marketing/api-reference-view";

export const metadata = { title: "API Reference — Pkay TDAI" };

export default function ApiReferencePage() {
  return <ApiReferenceView />;
}
