import { HowItWorksView } from "@/components/marketing/how-it-works-view";

export const metadata = { title: "How it works — Pkay TDAI" };

export default function HowItWorksPage() {
  return <HowItWorksView />;
}
