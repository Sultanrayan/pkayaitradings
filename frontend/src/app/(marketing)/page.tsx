import { HomeView } from "@/components/marketing/home-view";

export const metadata = { title: "Pkay TDAI — Multi-Agent Market Intelligence" };

export default function HomePage() {
  return <HomeView />;
}
