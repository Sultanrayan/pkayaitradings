[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pkay-tdai"
version = "0.1.0"
description = "Multi-agent AI system for real-time Gold (XAUUSD) and Bitcoin (BTCUSD) market analysis."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Pkay TDAI Contributors" }]
keywords = ["trading", "multi-agent", "technical-analysis", "quant", "biquote"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Investment",
]
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "httpx>=0.27",
    "websockets>=12.0",
    "tenacity>=8.3",
    "structlog>=24.1",
    "pandas>=2.2",
    "numpy>=1.26",
    "redis>=5.0",
    "celery>=5.4",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "alembic>=1.13",
]

[project.optional-dependencies]
ml = [
    "scikit-learn>=1.5",
    "xgboost>=2.0",
    "torch>=2.3",
]
llm = [
    "openai>=1.35",
    "anthropic>=0.34",
]
vector = [
    "qdrant-client>=1.9",
]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "respx>=0.21",
    "ruff>=0.5",
    "mypy>=1.10",
    "pandas-stubs>=2.2",
    "types-redis",
]

[project.urls]
Homepage = "https://github.com/your-org/pkay-tdai"
Repository = "https://github.com/your-org/pkay-tdai"

[tool.setuptools]
packages = [
    "agents",
    "agents.technical_analyst",
    "agents.technical_analyst.models",
    "agents.news_monitor",
    "agents.news_monitor.scrapers",
    "agents.risk_manager",
    "agents.decision_maker",
    "shared",
    "shared.schemas",
    "shared.database",
    "shared.vector_store",
    "shared.utils",
    "data_pipeline",
    "execution",
    "orchestrator",
    "Raggrap",
    "Raggrap.config",
    "Raggrap.src",
    "Raggrap.src.core",
    "Raggrap.src.models",
]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra --strict-markers --strict-config"
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = ["error::DeprecationWarning"]
markers = [
    "integration: tests that require external services (Redis, Postgres, network)",
]

[tool.coverage.run]
branch = true
source = ["agents", "shared", "data_pipeline", "execution", "orchestrator"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@overload",
    "\\.\\.\\.",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["."]
exclude = [
    ".venv", "venv", "frontend", "Research", "build", "dist",
    "Raggrap/research", "Raggrap/Img",
]

[tool.ruff.lint]
select = [
    "E", "W", "F", "I", "N", "UP", "B", "C4", "SIM", "RET", "PTH", "RUF",
]
ignore = [
    "E501",
]

[tool.ruff.lint.per-file-ignores]
# The Raggrap engine lives in its own documented project folder ("Raggrap/").
"Raggrap/**" = ["N999"]
"tests/**" = ["S101"]

[tool.ruff.lint.isort]
known-first-party = ["agents", "shared", "data_pipeline", "execution", "orchestrator"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_ignores = true
warn_redundant_casts = true
disallow_untyped_defs = true
plugins = ["pydantic.mypy"]
files = ["agents", "shared", "data_pipeline", "execution", "orchestrator", "Raggrap"]
exclude = ["venv", "\\.venv", "build", "dist", "Raggrap/research", "Raggrap/Img"]

[[tool.mypy.overrides]]
module = ["celery.*", "qdrant_client.*", "openai.*", "anthropic.*", "xgboost.*", "torch.*", "sklearn.*"]
ignore_missing_imports = true

# Optional Raggrap dependencies: the engine falls back to in-memory/deterministic
# providers at runtime, so these SDKs may be absent under strict CI.
[[tool.mypy.overrides]]
module = ["spacy", "chromadb", "neo4j", "redis", "Raggrap.*"]
ignore_missing_imports = true
